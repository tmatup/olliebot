
Input:
  - userPick (number): optional, a number from 1 to 1,000,000, if not provided, randomly generate one for user

Output:
  - result (boolean): whether user has won the lottery.

Logic:
This tool will generate a random number from 1 to 1,000,000.
And it will compare the generated number with userPick.
Return the following info:
1. isWinner: true user has won the lottery, false otherwise.
2. user pick
3. winniner number
4. message: an appropriate message for user depending on whether they have won the lottery.