# Vite environment variables
# Variables prefixed with VITE_ are exposed to the client

# Enable WebSocket proxy through Vite (useful when backend runs on same port)
VITE_USE_WS_PROXY=false

# VITE_WS_URL can be used to explicitly set the WebSocket URL
# VITE_WS_URL=wss://example.com/ws